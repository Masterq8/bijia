<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skzh.visits.mapper.visitsMapper">
    <resultMap type="visits" id="SysLogininforResult">
        <result property="infoId" column="info_id"/>
        <result property="userName" column="user_name"/>
        <result property="status" column="status"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>

    </resultMap>
    <sql id="selectOrderInfoVo">
        SELECT DATE(login_time) AS date, COUNT(*) AS count FROM sys_logininfor GROUP BY date ORDER BY date asc
    </sql>

    <select id="getDailyVisits" resultType="java.util.Map">
        <include refid="selectOrderInfoVo"/>
    </select>

</mapper>
